return {
    'numToStr/Comment.nvim',
    opts = {},
}

-- keybinds
-- gcc -> line into comment
-- gbc -> block into comment
-- gc - gb when in visual mode
-- other uses are: [count]gcc, gc[count][direction]
-- works for gbc and gb too

-- gcw -> word into comment
-- gco - gcO -> insert new line below/above into comment
-- gcA -> insert at the end of line into comment
